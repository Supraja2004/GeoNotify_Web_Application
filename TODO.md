# TODO List for Adding Delete Functionality to GeoNotify

- [x] Add DELETE /api/geofences/:id route in server.js to remove a geofence by ID.
- [x] Update loadFences function in app.js to include a "Delete" button in each polygon's popup.
- [x] Test deletion by creating a fence, loading, and deleting it.
- [x] Ensure UI updates correctly after deletion.
